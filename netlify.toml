# Build settings for Netlify (Cloudflare Pages: enter the same two values in its UI).
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Vite 8 needs Node 20.19+ or 22.12+.
  NODE_VERSION = "22"
